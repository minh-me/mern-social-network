import { FC } from 'react';
import { Title } from 'components/App';

type Props = {};

export const SearchPage: FC<Props> = (props) => {
  return (
    <>
      <Title title="Search" />
    </>
  );
};
