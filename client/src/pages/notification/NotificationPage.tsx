import { Title } from 'components/Common';
import { FC } from 'react';

type Props = {};

export const NotificationPage: FC<Props> = (props) => {
  return (
    <>
      <Title title="Notification" />
    </>
  );
};
