import { FC } from 'react';
import { Title } from 'components/Common';

type Props = {};

export const ProfilePage: FC<Props> = (props) => {
  return (
    <>
      <Title title="Profile" />
    </>
  );
};
