import { FC } from 'react';
import { Title } from 'components/App';

type Props = {};

export const ProfilePage: FC<Props> = (props) => {
  return (
    <>
      <Title title="Profile" />
    </>
  );
};
