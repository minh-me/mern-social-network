import { Typography } from '@mui/material';

type Props = {};

export const CommentText = (props: Props) => {
  return (
    <Typography fontSize={12} color="#e4e6ed">
      Seriously? Do the players, bare Ronaldo, DeGea and a few others care to play for the club?
    </Typography>
  );
};
